<h2>Listing opmls</h2>

<table class="table">
  <thead>
    <tr>
      <th>User</th>
      <th>Content type</th>
      <th>Filename</th>
      <th>inserted at</th>
      <th>Path</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for opml <- @opmls do %>
    <tr>
      <td><%= link opml.user.name, to: user_path(@conn, :show, opml.user) %></td>
      <td><%= opml.content_type %></td>
      <td><%= opml.filename %></td>
      <td><%= opml.inserted_at %></td>
      <td><%= opml.path %></td>

      <td class="text-right">
        <%= link [fa_icon("cogs"), " Parse"], to: opml_path(@conn, :import, opml),
                           class: "btn btn-info btn-xs" %> &nbsp;
        <%= link "Show", to: opml_path(@conn, :show, opml), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: opml_path(@conn, :edit, opml), class: "btn btn-default btn-xs" %>
        <%= link "Delete", to: opml_path(@conn, :delete, opml), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New opml", to: opml_path(@conn, :new) %>
