<h2>Update Password</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_frontend_path(@conn, :update) %>

<%= link "Back", to: user_frontend_path(@conn, :profile) %>
