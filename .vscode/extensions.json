{
  // See https://go.microsoft.com/fwlink/?LinkId=827846 for the format.
  "recommendations": [
    // Elixir language server (Next LS) + formatter, linting, HEEx support
    "elixir-tools.elixir-tools",
    // Phoenix HEEx template syntax highlighting
    "phoenixframework.phoenix",
    // Tailwind CSS IntelliSense (class autocomplete in .heex / app.css)
    "bradlc.vscode-tailwindcss"
  ],
  // ElixirLS provides the same LSP features as elixir-tools plus a debugger,
  // but the two conflict if both are enabled at once — pick one.
  "unwantedRecommendations": [
    "JakeBecker.elixir-ls"
  ]
}
