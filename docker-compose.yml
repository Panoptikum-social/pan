services:
  app:
    build: .
    restart: unless-stopped
    ports:
      # host:container — change the host side if 4001 is already taken
      - "4001:4000"
    environment:
      # See .env.example — docker compose loads .env automatically and
      # substitutes these; POSTGRES_PASSWORD is shared with the db service
      # below so it only needs setting in one place. Consumed by
      # config/runtime.exs's `config_env() == :qa` branch.
      POSTGRES_USER: postgres
      POSTGRES_DB: pan_qa
      POSTGRES_HOST: db
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is not set, see .env.example}
      SECRET_KEY_BASE: ${SECRET_KEY_BASE:?SECRET_KEY_BASE is not set, see .env.example}
      PAN_HOST: ${PAN_HOST:?PAN_HOST is not set, see .env.example}
      PAN_PORT: ${PAN_PORT:-4001}
    volumes:
      # matches the hardcoded /var/phoenix/pan-uploads path in
      # lib/pan_web/**/opml_controller.ex
      - uploads:/var/phoenix/pan-uploads
    depends_on:
      db:
        condition: service_healthy
      search:
        condition: service_healthy

  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: pan_qa
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is not set, see .env.example}
    volumes:
      - db_data:/var/lib/postgresql/data
      # seeds a baseline schema + demo data on first init (empty data dir
      # only) — running the full migration history from a truly empty DB
      # fails partway through, see DOCKER.md
      - ./materials/pan_dev.sql.gz:/docker-entrypoint-initdb.d/01-pan_dev.sql.gz:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  search:
    image: manticoresearch/manticore:latest
    restart: unless-stopped
    # not published to the host — only the app container needs to reach it,
    # via the "search" service name (see config/qa.exs :manticore_url)
    volumes:
      - search_data:/var/lib/manticore
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    healthcheck:
      # the manticoresearch/manticore image has no curl/wget; the mysql
      # socket appearing is a reliable proxy for "accepting connections"
      test: ["CMD-SHELL", "test -S /var/run/mysqld/mysqld.sock"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  db_data:
  search_data:
  uploads:
